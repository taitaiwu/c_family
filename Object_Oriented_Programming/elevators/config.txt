# =====================================
# 電梯模擬系統 - 外部設定檔
# 格式: KEY = VALUE
# 以 # 開頭的行視為註解，會被忽略
# =====================================

# 大樓樓層總數(至少需要1)
FLOOR_COUNT = 10

# 電梯數量(至少需要2)
ELEVATOR_COUNT = 3

# 每部電梯的載重上限 (kg)
ELEVATOR_CAPACITY = 800

# 每個樓層、每個時間單位產生乘客的機率 (0.0 ~ 1.0)
ARRIVAL_PROBABILITY = 0.15

# 模擬總時間長度 (時間單位數，例如可視為秒)
SIMULATION_DURATION = 300

# 電梯開門後停留時間 (時間單位數)
DOOR_DWELL_TIME = 3

# 亂數種子。設為 0 表示每次執行使用不同的隨機結果（依系統時間決定）
# 設為任意非 0 整數，可讓模擬結果可重現（方便除錯與報告比對）
RANDOM_SEED = 0
